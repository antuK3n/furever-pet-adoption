@model PetListViewModel
@{
    ViewData["Title"] = "Find a Pet";
    var statusOptions = new[] { "Available", "Reserved", "Adopted", "Medical Hold", "All" };
    var showingDefault = Model.Status == "Available";
    var hasFilters = !string.IsNullOrEmpty(Model.Species) || !string.IsNullOrEmpty(Model.Search) || !showingDefault;
}

<div class="section">
    <h1>Find a pet</h1>
    <p class="muted">Browse our pets — filter by status or species, or search by name, breed, or temperament.</p>

    <form method="get" class="filter-bar" role="search" aria-label="Filter pets" data-pet-filter>
        <div class="form-group">
            <label class="form-label" for="status">Status</label>
            <select id="status" name="status" class="input">
                @foreach (var st in statusOptions)
                {
                    <option value="@st" selected="@(st == Model.Status)">@(st == "All" ? "All statuses" : st)</option>
                }
            </select>
        </div>
        <div class="form-group">
            <label class="form-label" for="species">Species</label>
            <select id="species" name="species" class="input">
                <option value="">All species</option>
                @foreach (var s in Model.SpeciesOptions)
                {
                    <option value="@s" selected="@(s == Model.Species)">@s</option>
                }
            </select>
        </div>
        <div class="form-group">
            <label class="form-label" for="search">Search</label>
            <input id="search" type="search" name="search" class="input" value="@Model.Search"
                   placeholder="Name, breed, or temperament" autocomplete="off" data-live-search />
        </div>
        <noscript><button type="submit" class="btn btn-primary">Filter</button></noscript>
        @if (hasFilters)
        {
            <a class="btn btn-secondary" asp-action="Index">Clear</a>
        }
    </form>

    @if (Model.Pets.Count > 0)
    {
        var statusLabel = Model.Status == "All" ? "" : Model.Status.ToLower() + " ";
        <p class="muted text-sm"><span data-result-count>@Model.Pets.Count</span> @statusLabel pet@(Model.Pets.Count == 1 ? "" : "s")</p>
        <div class="pet-grid" data-pet-results>
            @foreach (var pet in Model.Pets)
            {
                <partial name="_PetCard" model="pet" />
            }
        </div>
        <div class="empty-state is-hidden" data-no-match>
            <div class="empty-glyph" aria-hidden="true"><partial name="_Glyph" model='"search"' /></div>
            <h3>No pets match your search</h3>
            <p>Try a different name, breed, or temperament.</p>
        </div>
    }
    else
    {
        <div class="empty-state">
            <div class="empty-glyph" aria-hidden="true"><partial name="_Glyph" model='"search"' /></div>
            <h3>No pets match your search</h3>
            <p>Try a different species or clear your filters.</p>
            <a class="btn btn-primary" asp-action="Index">Show all pets</a>
        </div>
    }
</div>
